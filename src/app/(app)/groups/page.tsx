import * as React from "react";
import GroupsClient from "./GroupsClient";

export const metadata = {
  title: "GoRiderss — Groups",
};

export default function Page() {
  return <GroupsClient />;
}
